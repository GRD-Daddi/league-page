- [Yahoo hidden manager GUIDs](yahoo-hidden-guids.md) — Yahoo masks manager guid/nickname as "--hidden--"; join teams↔users on team_key, never guid.
- [Player dataset field shape](player-data-shape.md) — page-level player map uses Sleeper-style fn/ln/pos/t, not Yahoo full_name/position/team
- [yahoo-fantasy client-bundle leak](yahoo-fantasy-client-bundle.md) — Node-only yahoo-fantasy leaks into browser bundle via helper.js barrel; stub it in vite for non-SSR resolution.
- [Yahoo season-finished status](yahoo-season-finished.md) — draft_status never goes "complete"; use league is_finished==1 to detect season end.
- [Champion auto-record vs manual](champion-auto-record.md) — season_records.champion_source 'auto'/'manual'; manual override never overwritten by auto-detection.
- [Yahoo adapter quirks](yahoo-adapter-quirks.md) — library api() unguarded JSON.parse crashes server on plain-text body → use rawYahooGet; traded picks need raw fetch; rosters is a map; offseason /teams,/standings fail → enumerate /team/{key}; past seasons via `renew` chain.
- [Session & token refresh design](session-auth-design.md) — sessions in Postgres, cookie holds only an opaque id; Yahoo rotates refresh_token on use so refresh MUST be serialized + DB-re-read before logout.
- [OAuth in Replit iframe & auth guards](oauth-iframe-and-guards.md) — third-party OAuth cannot complete in the preview iframe; guards must redirect in-app, not to external login. Rewrite does not fix it.
- [Pot throne + history archive](pot-throne-archive.md) — "person to beat"=last season champ unless pot claimed; awardPot ledger year MUST equal reigning year or throne won't reset.
- [Matchup archive playoff coverage](matchup-archive-playoff-coverage.md) — durable season matchup snapshots must use loadAllSeasonMatchups (all weeks incl playoffs), not loadMatchupData (regular season only).
- [Draft pick ownership model](draft-pick-ownership.md) — upcoming-draft picks board is manual counts-per-round (commissioner-entered), not Yahoo; don't replace with Yahoo predicted picks.
- [Yahoo league history archive](yahoo-history-archive.md) — recover real multi-season Yahoo history: enumerate by name + raw REST, not renew chains or offseason library helpers.
- [Indentation conventions](indentation-conventions.md) — src/routes/** = TABS; src/lib/server/*.js = 8-space (no tabs). Match the sibling or get noisy diffs.
- [Owner-grouped league history](owner-vs-team-history.md) — history pages group by OWNER (manager_name nickname), not team_name; wrap nicknames in ownerDisplayName(); records join matchup_archive→team_season_archive on year+team_key.
- [Stale build vs logic bug](stale-build-vs-logic-bug.md) — "UI shows old data" with correct code: git-diff screenshot vs HEAD~N + check deployment build before debugging; republish needed for code, not data.
- [Vite stale dep cache breaks hydration](vite-stale-dep-cache-after-merge.md) — interactive UI "doesn't work anymore" + optimize-deps chunk 404s in logs (often post task-merge) = stale node_modules/.vite; rm -rf it + restart; verify in browser not curl.
- [Svelte5 form $state SSR init](svelte5-form-state-ssr-init.md) — editable inputs populated only via $effect render defaults during SSR/broken hydration → "fields show 0, won't save" while ledger/DB are correct; init $state from data.* directly.
- [Per-season history page archive views](history-page-archive-views.md) — past-year standings/matchups render from archive (skip Yahoo); records cards deep-link game→/matchups?year=&week=&matchup=, season→/standings?year=&team=.
- [Payouts-driven pot model](payouts-driven-pot-model.md) — commissioner enters 1st/2nd/3rd payouts in $; pool=sum, pool_share=pool/members drives split; $ authoritative, pct derived for display.
- [final_rank semantics & corrupted-season repair](archive-final-rank-vs-corruption.md) — final_rank=FINAL playoff placement (≠ reg-season leader); a botched season backfill (zeroed stats/bogus ranks/dup next-year-key rows) is repairable from matchup_archive + season_archive podium via psql, no code change.
- [Homepage auth gating & pot-value sync](homepage-auth-and-pot-sync.md) — homepage is the only public route; gate league data (incl. nav pot pill) on auth in loader+template; "the pot" everywhere must use projected-vs-actual headline rule, not actual-only.
- [Playoff bracket derivation](playoff-bracket-derivation.md) — split playoff games into champ/consol via union-find over playoff matchups (each component=a bracket); champion component = lowest final_rank. No seed-cutoff exists.
- [Live matchups Yahoo bracket mismatch](live-matchups-brackets-yahoo-mismatch.md) — current-year /matchups uses legacy Sleeper MatchupsAndBrackets; loadBrackets (Yahoo) has no `champs` → crashed tab. Live playoff bracket UI is dead under Yahoo; only archived path has champ/consol split.
- [Yahoo→Sleeper player id bridge](yahoo-sleeper-player-id-bridge.md) — Yahoo player_key "{game}.p.{id}" ≠ Sleeper-keyed players map; bridge via Sleeper `yahoo_id` (numeric after ".p."). Used for draft picks; never print raw key.
- [Pot projection from uncollected buy-ins](pot-projection-uncollected-funds.md) — upcoming-season pot/pool projected from expected members (season_archive.num_teams), flagged Projected; real potTotal/getPotTotal stay collected-only.
- [Design system](design-system.md) — SMUI fully removed; build UI with plain Svelte + stadium.css (.sn-* classes) + LinearProgress.svelte, never reintroduce @smui.
- [Keepers engine](keepers-engine.md) — 3-season cap (draft=yr1), drop breaks lineage, cost=last-drafted round (waiver/FA=R6) sticky thru trades; keeper consumes a pick in the upcoming-year board.
- [Yahoo keeper flags source](yahoo-keeper-flags.md) — keeper status is on roster is_keeper.kept, NOT draftresults; league keeper since 2022; cost=first non-keeper round; import stamps is_keeper from rosters.
- [Keeper cross-season team_key bridge](keeper-cross-season-teamkey-bridge.md) — preseason rosters fall back to last season (new league key + reshuffled .t.N); remap to upcoming key by owner GUID (then unambiguous team name), never .t.N, or every team shows "no pick".
- [Yahoo transaction player-movement shape](yahoo-transaction-player-shape.md) — library puts player moves under player.transaction (not .transaction_data); reading wrong field → empty adds/drops → "0 transaction events" import & broken /transactions page.
- [Vote turnout roster join](vote-turnout-roster.md) — open-vote "who voted" roster joins Yahoo owners↔ballots; match GUID then stable id then display-name (guids hidden); choices stay anonymous always.
- [Past-season /transactions archive](transactions-past-season-archive.md) — past seasons reconstruct from transaction_archive (live Yahoo 999s on ~17 calls/view); names backfilled into player_name from public DynastyProcess crosswalk (skill players ~100%); DEF id→team map has no no-auth source (web search hallucinates) so unmapped DEFs show "Defense"; traded picks re-hydrated live via getTradedPicksByTransaction (league key from transaction_id).
- [Transactions page server digest](transactions-page-server-digest.md) — /transactions needs SERVER-SIDE digest of raw Yahoo txns into {id,date,season,type,rosters,moves}; bridge player_key→Sleeper via `yh`, synth unbridged from players_meta, teamManagersMap from team_season_archive + backfill referenced rosters, stale:false.
- [Svelte {#if} whitespace trim](svelte-if-block-whitespace-trim.md) — a space at an {#if}/{/if} boundary is trimmed → "TheLeague"; keep inline spacing inside a text expression `{lead ? lead+' ' : ''}`.
- [Config-driven branding](config-driven-branding.md) — app name/hero are config-driven off leagueName + homepageHeroSubtitle (leagueInfo.js, re-exported via helper.js); two-tone brand = brandLead/brandTail split on last space; static/manifest.json is the one hardcoded holdout.
- [Draft-picks UI is square grid](draft-picks-ui-canonical.md) — "Draft picks by round" on home + /keepers must be the .picks-grid/.pick-cell square grid, NOT pill chips; converge that direction.
- [Keeper per-team cap](keeper-max-cap.md) — pot_settings.max_keepers (default 2); cap accounting MUST use one all-selections metric (team.atLimit/totalSelected) across UI gate, save, and approval — never roster-filtered selectedCount.
- [Keepers perf / per-viewer cache](keepers-perf-cache.md) — Keep/Remove felt frozen because use:enhance reruns load() → refetches all Yahoo data each click; fix = 60s cache scoped by session.userId (never league-key alone) + Saving… button state.
- [Matchup drill-in player scores](matchup-detail-ondemand.md) — archive holds only team totals; per-player past scores fetched on-demand from Yahoo scoreboard via league key derived from team_key, sides matched by roster_id.
- [Transactions Yahoo digest contract](transactions-yahoo-digest.md) — legacy tx components need digested txns + teamManagersMap + Sleeper-keyed players; past-season team names come from team_season_archive, NOT flaky live Yahoo owner fetch.
- [Legacy team-managers SSR shape](legacy-teammanagers-ssr-shape.md) — SSR loaders rendering Yahoo data through getTeamFromTeamManagers-family must pass full {currentSeason,teamManagersMap,users} (loadLiveTeamManagers live / buildTransactionTeamManagers archive), never a flat users map; only /matchups live still uses legacy components, the other history pages render from archive.
- [SvelteKit route module exports](sveltekit-route-exports.md) — +page.server.js may only export load/actions/etc (or _-prefixed); extra named exports (e.g. test helpers) fail `vite build` though `vitest` passes. Extract to a sibling .js.
- [Archived schedule byes](archive-schedule-byes.md) — getArchivedSchedule games can have null home/away (byes); matchup UIs must render sides conditionally + drop VS divider, or a phantom "Unknown" opponent appears.
